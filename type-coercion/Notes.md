📝 Short Notes – Type Coercion
+ → String concatenation if one operand is string
-, *, / → Converts to number
== → Type conversion
=== → Strict comparison
NaN is never equal to itself
Empty array is truthyx